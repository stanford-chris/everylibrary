# Geograph bulk dump: 235 MB, re-downloadable, never commit it.
data/*.tsv.gz
data/*.mysql.gz

# Resumable run state; regenerable and noisy in diffs.
data/image_state.json
data/image_state.json.tmp

# Posted-library IDs, written on every run. Matches how every other bot's
# state file in this repo is handled. No .tmp line: everylibrary_post.py
# writes this one directly, unlike image_state.json.
data/post_state.json
data/_tmp/
data/describe_test.log

# Per-URL verdicts from everylibrary_urls.py. Regenerable, and it churns every
# time a council's WAF has a bad minute.
data/url_state.json
data/url_state.json.tmp

# Atomic-write scratch files. Both writers replace() over the real file, so
# these exist for an instant; a crash mid-write is the only way one survives.
data/uk_libraries_images.csv.tmp
data/uk_libraries_final.csv.tmp

# Run logs and hand-made state backups, never source: the logs are what
# the monthly and annual passes print, the .bak/.pre-* files are copies of
# post_state.json taken before a repost. Added 20 September 2026.
__pycache__/
.DS_Store
data/*.log
data/post_state.json.*
